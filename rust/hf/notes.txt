Need to move all the core function of th SCF solver into lib.rs, so that main.rs just calls it (and builds a local binary). Then #[wasm_bindgen] is very similar to main.rs in that it calls the SCF solver
